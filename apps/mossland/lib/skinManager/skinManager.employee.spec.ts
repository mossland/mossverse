describe("SkinManager Service", () => {
  // const system = new TestSystem();
  // let skinManagerEmployee: SkinManagerEmployee;
  // beforeAll(async () => {
  //   const app = await system.init(registerModules(environment));
  //   skinManagerEmployee = app.get<SkinManagerEmployee>(SkinManagerEmployee);
  // });
  // afterAll(async () => await system.terminate());
  // let skinManager: db.SkinManager.Doc;
  // let input: cnst.SkinManagerInput;
  // it("Create SkinManager", async () => {
  //   input = sample.skinManagerInput();
  //   skinManager = await skinManagerEmployee.create(input);
  //   expect(skinManager.status).toEqual("active");
  // });
  // it("Update SkinManager", async () => {
  //   input = sample.skinManagerInput();
  //   skinManager = await skinManagerEmployee.update(skinManager._id, input);
  // });
  // it("Remove SkinManager", async () => {
  //   skinManager = await skinManagerEmployee.remove(skinManager._id);
  //   expect(skinManager.status).toEqual("inactive");
  // });
});
