"use client";
// import { YoptaRenderer } from "@yoopta/editor";

export interface EditorProps {
  className?: string;
  data?: any;
}

export const BlockRenderer = ({ className, data }: EditorProps) => {
  return <></>;
  // return <YoptaRenderer className={className} data={data} />;
};
